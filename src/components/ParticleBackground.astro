---

---

<canvas id="particle-bg"></canvas>

<style>
  #particle-bg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    pointer-events: none;
    background: var(--color-bg, #fafafa);
  }
</style>

<script>
  const canvas = document.getElementById("particle-bg") as HTMLCanvasElement;
  const ctx = canvas.getContext("2d");

  let width: number = 0;
  let height: number = 0;
  let particles: Particle[] = [];

  // Configuration
  // Balanced Cute: Dusty Pink, Soft Lilac, Pale Blue - Visible but soft
  const colors = [
    "235, 180, 195", // Dusty Pink (Visible against white)
    "220, 190, 240", // Soft Lilac
    "200, 220, 255", // Pale Blue
    "255, 200, 210", // Rose
  ];

  const particleCountMobile = 30;
  const particleCountDesktop = 50;

  function resize() {
    width = window.innerWidth;
    height = window.innerHeight;
    canvas.width = width;
    canvas.height = height;
  }

  class Particle {
    x: number;
    y: number;
    vx: number;
    vy: number;
    size: number;
    color: string;
    opacity: number;
    fadingIn: boolean;
    fadeSpeed: number;

    constructor() {
      this.x = Math.random() * width;
      this.y = Math.random() * height;
      this.vx = (Math.random() - 0.5) * 0.4; // Gentle movement
      this.vy = (Math.random() - 0.5) * 0.4;
      this.size = Math.random() * 3 + 2; // 2 to 5px - Visible but not huge
      this.color = colors[Math.floor(Math.random() * colors.length)];
      this.opacity = Math.random() * 0.4 + 0.3; // 0.3 to 0.7 - Clearly visible but transparent
      this.fadeSpeed = Math.random() * 0.005 + 0.002;
      this.fadingIn = Math.random() > 0.5;
    }

    update() {
      this.x += this.vx;
      this.y += this.vy;

      // Wrap around screen
      if (this.x < 0) this.x = width;
      if (this.x > width) this.x = 0;
      if (this.y < 0) this.y = height;
      if (this.y > height) this.y = 0;

      // Pulse opacity
      if (this.fadingIn) {
        this.opacity += this.fadeSpeed;
        if (this.opacity >= 0.8) this.fadingIn = false;
      } else {
        this.opacity -= this.fadeSpeed;
        if (this.opacity <= 0.2) this.fadingIn = true;
      }
    }

    draw() {
      if (!ctx) return;
      ctx.fillStyle = `rgba(${this.color}, ${this.opacity})`;
      ctx.beginPath();
      ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
      ctx.fill();

      // Optional: Add a subtle glow
      // ctx.shadowBlur = 5;
      // ctx.shadowColor = `rgba(${this.color}, 0.5)`;
    }
  }

  function initParticles() {
    particles = [];
    const count = width < 768 ? particleCountMobile : particleCountDesktop;
    for (let i = 0; i < count; i++) {
      // @ts-ignore
      particles.push(new Particle());
    }
  }

  function animate() {
    if (!ctx) return;
    ctx.clearRect(0, 0, width, height);

    // Update and draw particles
    for (let i = 0; i < particles.length; i++) {
      particles[i].update();
      particles[i].draw();
    }

    requestAnimationFrame(animate);
  }

  window.addEventListener("resize", () => {
    resize();
    initParticles();
  });

  // Init
  resize();
  initParticles();
  animate();
</script>
