---
title: 华为2026-04-23 机试真题（留学生AI方向）
date: 2026-04-23
category: Internship/HUAWEI
tags:
  - 笔试
  - 华为
  - AI
  - 留学生
description: 华为2026年4月23日留学生AI方向机试真题，含20道选择题和2道编程题。
status: published
---

# 华为 2026-04-23 机试真题（留学生AI方向）

来源：[CodeFun2000](https://codefun2000.com)

---

## 第1题-选择题（P4829）共20道，1-15单选，16-20多选

### 1. DDP训练中每个GPU计算完局部梯度后，需执行什么通信操作保持模型权重一致？
- A. All-Gather
- B. Broadcast
- C. Reduce-Scatter
- D. All-Reduce

:::fold[答案]

正确答案：D
解析：DDP中每个GPU独立计算局部梯度后，通过All-Reduce操作对所有GPU的梯度求和并广播，确保各GPU在使用相同梯度更新后权重保持一致。
:::

### 2. 使用K-Means算法聚类之前必须预先指定的关键参数是？
- A. 学习率
- B. 距离度量方式
- C. 聚类数K
- D. 最大迭代次数

:::fold[答案]

正确答案：C
解析：K-Means必须预先指定聚类数K，这是算法的核心超参数。学习率不是K-Means的参数，距离度量默认欧氏距离，最大迭代次数有默认值。
:::

### 3. 数据矩阵$X \in \mathbb{R}^{n \times d}$，线性映射$Z=XW$（$W \in \mathbb{R}^{d \times k}$），则新矩阵$Z$的维度为？
- A. $\mathbb{R}^{n \times d}$
- B. $\mathbb{R}^{d \times k}$
- C. $\mathbb{R}^{k \times d}$
- D. $\mathbb{R}^{n \times k}$

:::fold[答案]

正确答案：D
解析：矩阵乘法 $(n \times d) \times (d \times k) = (n \times k)$，因此 $Z$ 的维度为 $\mathbb{R}^{n \times k}$，将 $d$ 维特征映射到 $k$ 维空间。
:::

### 4. 输入向量$[4,4,4]$经Softmax函数处理后输出结果为？
- A. $[1/3,1/3,1/3]$
- B. $[0.25,0.25,0.5]$
- C. $[1,0,0]$
- D. $[4/12,4/12,4/12]$

:::fold[答案]

正确答案：A
解析：Softmax输入各分量相等时，输出为均匀分布。$\text{softmax}([4,4,4]) = [e^4/(3e^4), e^4/(3e^4), e^4/(3e^4)] = [1/3, 1/3, 1/3]$。
:::

### 5. K-Means算法衡量样本点归属的核心准则是？
- A. 余弦相似度最大
- B. 曼哈顿距离最小
- C. 欧氏距离最小
- D. 马氏距离最小

:::fold[答案]

正确答案：C
解析：标准K-Means使用欧氏距离衡量样本点到聚类中心的距离，将样本分配给距离最近的聚类中心。其他距离度量是非标准变体。
:::

### 6. 特征工程中通常用于减少特征维度并保留主要信息的操作是？
- A. 数据增强
- B. 归一化
- C. 主成分分析（PCA）
- D. One-Hot编码

:::fold[答案]

正确答案：C
解析：PCA通过线性变换将数据投影到方差最大的方向（主成分），在减少特征维度的同时尽可能保留数据的主要信息，是最常用的线性降维方法。
:::

### 7. RAG系统中查询Query被转化为嵌入向量$\mathbf{q}=[1,2]^T$，数据库中存储$\mathbf{d}_1=[1,0]^T$和$\mathbf{d}_2=[0,1]^T$。采用内积检索哪个文档更相关？
- A. $\mathbf{d}_1$
- B. $\mathbf{d}_2$
- C. 相关性相同
- D. 无法判断

:::fold[答案]

正确答案：B
解析：内积 $\mathbf{q} \cdot \mathbf{d}_1 = 1 \times 1 + 2 \times 0 = 1$，$\mathbf{q} \cdot \mathbf{d}_2 = 1 \times 0 + 2 \times 1 = 2$，$2 > 1$，因此 $\mathbf{d}_2$ 更相关。
:::

### 8. $X_1,\ldots,X_n \sim N(\mu,\sigma^2)$，$\mu$未知$\sigma^2$已知，使似然函数最大的$\mu$值是？
- A. 样本中位数
- B. 样本方差
- C. 样本均值
- D. 总体均值

:::fold[答案]

正确答案：C
解析：正态分布下$\mu$的MLE是样本均值$\bar{X} = \frac{1}{n}\sum X_i$。总体均值是真实参数而非估计值，样本中位数在正态分布下也是一致估计但非MLE。
:::

### 9. 线性回归中关于$R^2$（决定系数），下列说法正确的是
- A. $R^2$取值范围为0到1
- B. $R^2$越大模型拟合效果越好
- C. $R^2$可以小于0
- D. $R^2$衡量的是预测值与真实值之间的相关系数

:::fold[答案]

正确答案：B
解析：$R^2$衡量模型解释的变异比例，值越大拟合效果越好。$R^2$通常取值在$[0,1]$，但当模型拟合效果极差时可能为负值，不过这属于边界情况。
:::

### 10. 评测长上下文能力时，为避免模型靠"猜测/泛化"蒙对，常见的高质量题型设计是？
- A. 选择题
- B. 填空题
- C. 大海捞针（Needle in a Haystack）
- D. 判断题

:::fold[答案]

正确答案：C
解析：大海捞针测试将关键信息随机隐藏在长文本中，要求模型精确定位并回答，无法通过常识猜测，是评测长上下文能力的主流方法。
:::

### 11. 特征$X$与目标$Y$存在非线性相关性且$X$不服从正态分布时，最合适的特征选择方法？
- A. Pearson相关系数
- B. Spearman秩相关系数
- C. 卡方检验
- D. T检验

:::fold[答案]

正确答案：B
解析：Spearman秩相关系数是非参数方法，不假设正态分布，基于秩次计算相关性，能有效捕获单调非线性关系。Pearson仅度量线性相关性，T检验和卡方检验各有适用条件。
:::

### 12. 矩阵SVD无法直接应用于以下哪个场景？
- A. 矩阵压缩
- B. 推荐系统
- C. 含缺失值的矩阵补全
- D. 求矩阵的逆

:::fold[答案]

正确答案：C
解析：标准SVD要求矩阵元素完整，无法直接处理含缺失值的矩阵。矩阵补全需使用带掩码的矩阵分解（如Funk-SVD）或凸优化方法。
:::

### 13. SmoothQuant的核心思想是？
- A. 使用更小的量化位宽
- B. 逐通道缩放平滑激活和权重的量化难度
- C. 训练时模拟量化误差
- D. 对异常值进行裁剪

:::fold[答案]

正确答案：B
解析：SmoothQuant通过逐通道缩放（smoothing factor），将激活值中的量化难点转移到更容易量化的权重上，使两者都能用INT8表示，实现无损的W8A8量化。
:::

### 14. 实数矩阵$A$的零空间是指？
- A. 满足$A\mathbf{x}=0$的所有向量$\mathbf{x}$的集合
- B. 矩阵$A$的所有列向量张成的空间
- C. 矩阵$A$的所有行向量张成的空间
- D. 矩阵$A$的值域

:::fold[答案]

正确答案：A
解析：零空间（Null Space）定义为 $\{\mathbf{x} \mid A\mathbf{x} = \mathbf{0}\}$。B为列空间，C为行空间，D为列空间（值域）。
:::

### 15. 神经网络相比线性模型的优势在于
- A. 训练速度更快
- B. 具有非线性表达能力
- C. 参数数量更少
- D. 不需要训练数据

:::fold[答案]

正确答案：B
解析：神经网络通过非线性激活函数和深层结构获得非线性表达能力，能学习复杂的非线性映射，这是其相比线性模型最核心的优势。
:::

### 16.（多选）Newton-Schulz迭代法求矩阵逆，正确的说法是？
- A. 适用于非奇异矩阵
- B. 每步迭代包含矩阵乘法运算
- C. 收敛速度是线性的
- D. 需选取合适的初始矩阵以保证收敛

:::fold[答案]

正确答案：A, B, D
解析：Newton-Schulz迭代法适用于非奇异（可逆）矩阵，每步需执行矩阵乘法，需选取合适初始矩阵（如 $\alpha A^T$）以保证收敛。收敛速度为二次（quadratic）而非线性。
:::

### 17.（多选）属于"自适应学习率"方法的是？
- A. SGD
- B. Adam
- C. AdaGrad
- D. RMSprop

:::fold[答案]

正确答案：B, C, D
解析：Adam、AdaGrad和RMSprop都根据梯度历史自动调整每个参数的学习率，属于自适应学习率优化器。标准SGD使用固定学习率。
:::

### 18.（多选）关于神经网络的层，正确的说法是？
- A. 线性层可表示为$Y=XW+b$
- B. 卷积层具有参数共享特性
- C. 归一化层可加速训练
- D. 残差连接可缓解梯度消失

:::fold[答案]

正确答案：A, B, C, D
解析：四个说法均正确。线性层即全连接层；卷积核在整个输入上滑动实现参数共享；BatchNorm/LayerNorm可加速收敛；残差连接通过恒等映射使梯度直接回传，缓解梯度消失。
:::

### 19.（多选）设$X$和$Y$是随机变量，$a$、$b$、$c$是常数，关于期望性质正确的有？
- A. $E(aX+b)=aE(X)+b$
- B. $E(X+Y)=E(X)+E(Y)$
- C. $E(XY)=E(X)E(Y)$（$X,Y$独立时）
- D. $E((X-c)^2)=\text{Var}(X)+(E(X)-c)^2$

:::fold[答案]

正确答案：A, B, C, D
解析：四个等式均正确。A是线性性质，B是期望可加性，C是独立时乘积的期望，D可通过展开 $E((X-c)^2)=E(X^2)-2cE(X)+c^2$ 并代入 $\text{Var}(X)=E(X^2)-E(X)^2$ 验证。
:::

### 20.（多选）关于插值多项式的说法，正确的是？
- A. $n+1$个点可唯一确定不超过$n$次的多项式
- B. Lagrange插值和Newton插值是同一多项式的不同表示
- C. 高次插值可能产生Runge现象
- D. 分段低次插值可避免振荡

:::fold[答案]

正确答案：A, B, C, D
解析：四个说法均正确。A是插值多项式基本定理；Lagrange和Newton只是计算形式不同；高次多项式插值在等距节点下可能出现Runge现象；分段低次插值（如样条）可有效避免振荡。
:::

---

## 第2题-基于最大边际相关性（MMR）的智能示例重排序（P4830）| 中等 | 2000ms

### 题目描述

你正在开发一个智能学习助手，它能够根据用户的问题从知识库中检索相关示例来帮助理解。为了提供既相关又多样化的示例，避免返回大量内容相似的重复结果，你决定采用最大边际相关性（Maximal Marginal Relevance, MMR）算法对候选文档进行重排序。MMR 在保证与查询相关的同时，通过惩罚与已选文档过于相似的候选，提升结果的多样性。

给定 N 个候选文档，每个文档有一个唯一的 ID 和一个与用户查询的相关性分数 rel[i]（范围 [0, 1]）。同时，给出了文档之间的相似度矩阵 sim[i][j]（范围 [0, 1]），其中 sim[i][j] 表示文档 i 与文档 j 的相似度（满足对称性且对角线为 1）。你需要实现 MMR 算法，根据平衡参数 λ（0 ≤ λ ≤ 1）和需要返回的数量 K，输出按照 MMR 得分降序选择的文档 ID 列表（按选择顺序）。

#### MMR 算法定义

1. **初始化**：已选文档集合 S 为空集。
2. **迭代 K 次**（0 ≤ K ≤ N；如果 K = 0，输出空列表）：
   - 对于每个尚未被选中的文档 i，计算其当前边际相关性得分：
     **MMR(i) = λ * rel[i] - (1 - λ) * max_{j∈S} sim[i][j]**
   - 如果 S 为空，则 max 项定义为 0。
   - 选择 MMR 得分最高的文档加入 S。如果多个文档的得分相等，则选择 ID 较小的那个。
   - 将选中文档的 ID 按顺序记录到结果列表中。

### 输入格式

第一行：一个整数 N（1 ≤ N ≤ 1000），表示候选文档数量。

接下来 N 行：每行包含一个浮点数 rel 和一个整数 ID，分别表示第 i 个文档（按输入顺序，索引从 0 到 N-1）的相关性分数和文档 ID。文档 ID 互不相同，范围在 1 到 10^9 之间。

接下来 N 行：每行包含 N 个浮点数，构成相似度矩阵 sim。第 i 行第 j 列表示文档 i 与文档 j 的相似度。矩阵保证对称（sim[i][j] = sim[j][i]），对角线 sim[i][i] = 1.00。所有浮点数精确到小数点后 2 位。

最后一行：一个浮点数 λ（0 ≤ λ ≤ 1）和一个整数 K（0 ≤ K ≤ N），空格分隔。

### 输出格式

输出一行，包含 K 个整数，即被选中文档的 ID，按照选择的顺序排列，ID 之间用空格分隔。如果 K = 0，输出空行（仅输出换行）。

### 样例 1

**输入**
```
3
0.9 101
0.6 102
0.3 103
1.0 0.95 0.2
0.95 1.0 0.1
0.2 0.1 1.0
0.7 2
```

**输出**
```
101 103
```

**说明**：
- 初始 S 为空，MMR 得分仅由相关性决定：101(0.63), 102(0.42), 103(0.21) → 选 101。
- 更新后：102: MMR = 0.7×0.6 - 0.3×max(0.95) = 0.135；103: MMR = 0.7×0.3 - 0.3×max(0.2) = 0.15。选 103。
- MMR 成功选择了相关性较低但与 101 更不相似的 103，体现了多样性。

### 样例 2

**输入**
```
3
0.9 101
0.8 102
0.7 103
1.0 0.2 0.3
0.2 1.0 0.9
0.3 0.9 1.0
0.5 2
```

**输出**
```
101 102
```

**说明**：
- 初始 S 为空：101(0.45), 102(0.4), 103(0.35) → 选 101。
- 更新后：102: MMR = 0.5×0.8 - 0.5×max(0.2) = 0.3；103: MMR = 0.5×0.7 - 0.5×max(0.3) = 0.2。选 102。
- 输出顺序 [101, 102]。

### 数据范围

- 1 ≤ N ≤ 1000
- 0 ≤ K ≤ N
- 0 ≤ λ ≤ 1

## 解题思路
采用贪心迭代算法，每次从未选文档中选择MMR得分最高的。MMR(i) = λ * rel[i] - (1-λ) * max_{j∈S} sim[i][j]，当S为空时max项为0。K次迭代后按选择顺序输出文档ID。平局时选择ID较小的文档，使用浮点数比较带容差处理精度问题。

## 参考代码
```python
import sys

def solve():
    data = sys.stdin.read().strip().split()
    if not data:
        return
    it = iter(data)
    N = int(next(it))

    docs = []  # (rel, docId, originalIdx)
    for i in range(N):
        rel = float(next(it))
        docId = int(next(it))
        docs.append((rel, docId, i))

    sim = []
    for i in range(N):
        row = [float(next(it)) for _ in range(N)]
        sim.append(row)

    lam = float(next(it))
    K = int(next(it))

    if K == 0:
        print()
        return

    selected = []       # 已选中文档的原始索引
    remaining = set(range(N))

    for _ in range(K):
        bestIdx = -1
        bestScore = -float('inf')
        for i in remaining:
            maxSim = 0.0
            for j in selected:
                if sim[i][j] > maxSim:
                    maxSim = sim[i][j]
            score = lam * docs[i][0] - (1.0 - lam) * maxSim
            # 比较：得分更高，或得分相同但ID更小
            if score > bestScore + 1e-12:
                bestScore = score
                bestIdx = i
            elif abs(score - bestScore) < 1e-12:
                if docs[i][1] < docs[bestIdx][1]:
                    bestIdx = i

        selected.append(bestIdx)
        remaining.remove(bestIdx)

    result = [str(docs[i][1]) for i in selected]
    print(' '.join(result))

if __name__ == '__main__':
    solve()
```

## 第3题-决策树实现网络设备故障预测（P4831）| 困难 | 1000ms

### 题目描述

在大型网络运维系统中，交换机和路由器会持续上报关键"运行状态"，如 CPU 使用率、内存占用、丢包率、温度等。为了进行故障分析，需要先将这些指标转换为二值告警信号：

- 当指标值 < 阈值时，记为 0（正常）
- 当指标值 >= 阈值时，记为 1（异常）

例如：CPU 使用率 [50, 70, 60, 80, 90]，阈值为 85，转换后为：[0, 0, 0, 0, 1]。

当多个指标同时异常时，设备发生故障的概率会增加。现给定一组"原始运行数据（连续值）"及对应阈值，请你：

1. 将原始特征数据进行告警判断，转换为 0/1 特征；
2. 使用 ID3 决策树算法学习特征与设备故障之间的关系；
3. 对新的样本进行预测。

#### ID3 决策树算法公式

**信息熵计算公式**：
H(D) = -∑_{i∈D} p_i * log2(p_i)
其中，p_i 为第 i 类样本在数据集 D 中所占的比例。

**信息增益计算公式**：
Gain(D, A) = H(D) - H(D|A) = H(D) - ∑ (|D_v| / |D|) * H(D_v)
其中，H(D|A) 为特征 A 对数据集 D 的条件熵，D_v 是特征 A 取值为 v 的子集。

### 输入格式

第一行：两个整数 N 和 M
- N：训练样本数量
- M：特征数量

第二行：M 个浮点数，表示每个特征的阈值

接下来 N 行：每行包含 M + 1 个浮点数
- 前 M 个为原始特征值（连续值）
- 最后一个为标签（0 表示正常，1 表示故障）

下一行：待测样本数量 q

接下来 q 行：每行包含 M 个原始特征值

### 输出格式

输出 q 个待检测样本的预测类别，以空格分隔：0 1 1 0 ...

### 样例 1

**输入**
```
10 3
75.0 65.0 5.0
60.5 60.2 3.1 0
80.3 70.8 6.2 1
78.7 60.4 6.5 1
72.1 68.9 4.2 0
90.6 80.1 7.3 1
85.2 62.5 3.6 0
65.4 75.3 6.8 1
70.8 60.7 5.0 0
88.9 66.2 4.8 1
74.6 72.4 6.1 1
3
82.5 64.0 4.0
68.2 70.5 6.2
91.0 60.3 3.2
```

**输出**
```
0 1 0
```

**说明**：
1. 特征离散化：根据阈值 (75.0, 65.0, 5.0) 将连续值转为 0/1。
2. ID3 决策树构建过程：
   - 整体信息熵：标签有 6 个 1，4 个 0，H(S) = 0.971
   - 第一层选择根节点：Gain(F2) ≈ 0.610 最大，选 F2 为根节点
   - 第二层：F2=0 分支，Gain(F0) ≈ 0.322 和 Gain(F1) ≈ 0.322 相同，选 F0
   - F2=1 分支已纯，直接为叶节点
   - 第三层：F2=0 且 F0=1 分支，选 F1，划分已纯净
3. 最终构建的决策树：
```
       F2
     /    \
   F0      1
  /  \
 0    F1
     /  \
    0    1
```

### 样例 2

**输入**
```
8 3
75.5 68.0 4.5
60.2 55.1 2.0 0
82.3 70.5 6.1 1
78.0 65.2 5.0 1
69.5 72.3 3.8 0
90.1 85.0 7.2 1
74.0 60.0 4.0 0
88.8 66.6 5.5 1
72.2 69.1 4.8 1
3
80.0 60.0 3.0
85.0 75.0 6.0
70.0 80.0 5.0
```

**输出**
```
0 1 1
```

**说明**：
1. 特征离散化：根据阈值 (75.5, 68.0, 4.5) 将连续值转为 0/1。
2. ID3 决策树构建：
   - 整体信息熵：标签有 5 个 1，3 个 0，H(S) = 0.954
   - 信息增益：Gain(F0) ≈ 0.55, Gain(F1) ≈ 0.05, Gain(F2) ≈ 0.95（最大）
   - 选 F2 为根节点，划分后样本已完全纯净，构建完毕
   - 该样例为一层决策树，根节点为 F2

### 数据范围

- 1 ≤ N ≤ 10^3
- 1 ≤ M ≤ 20
- 1 ≤ q ≤ 10^3

## 解题思路
先将连续特征按阈值二值化（≥阈值为1，<阈值为0）。然后使用ID3算法递归构建决策树：每次选择信息增益最大的特征作为分裂节点，对每个取值划分数据集后递归建树。停止条件为所有样本同类、无剩余特征或信息增益为0。预测时沿树的分支走到叶节点输出类别。

## 参考代码
```python
import sys
from math import log2
from collections import Counter

def entropy(labels):
    """计算信息熵"""
    n = len(labels)
    if n == 0:
        return 0.0
    counter = Counter(labels)
    ent = 0.0
    for cnt in counter.values():
        p = cnt / n
        if p > 0:
            ent -= p * log2(p)
    return ent

def informationGain(data, labels, featureIdx):
    """计算某个特征的信息增益"""
    totalEnt = entropy(labels)
    n = len(data)
    subsetLabels = {0: [], 1: []}
    for i, row in enumerate(data):
        subsetLabels[row[featureIdx]].append(labels[i])
    condEnt = 0.0
    for val in [0, 1]:
        sub = subsetLabels[val]
        if sub:
            condEnt += (len(sub) / n) * entropy(sub)
    return totalEnt - condEnt

class Node:
    """决策树节点"""
    def __init__(self):
        self.featureIdx = -1       # 分裂特征索引，-1表示叶节点
        self.children = {}         # 特征值 -> 子节点
        self.label = -1            # 叶节点类别

def buildTree(data, labels, featureIndices):
    """递归构建ID3决策树"""
    # 所有样本标签相同
    if len(set(labels)) == 1:
        node = Node()
        node.label = labels[0]
        return node

    # 无剩余特征
    if not featureIndices:
        node = Node()
        node.label = Counter(labels).most_common(1)[0][0]
        return node

    # 选择最佳特征
    bestGain = -1.0
    bestFeature = None
    for f in featureIndices:
        gain = informationGain(data, labels, f)
        if gain > bestGain + 1e-12:
            bestGain = gain
            bestFeature = f

    if bestFeature is None or bestGain <= 1e-12:
        node = Node()
        node.label = Counter(labels).most_common(1)[0][0]
        return node

    node = Node()
    node.featureIdx = bestFeature
    remainingFeatures = [f for f in featureIndices if f != bestFeature]

    for val in [0, 1]:
        subData = []
        subLabels = []
        for i, row in enumerate(data):
            if row[bestFeature] == val:
                subData.append(row)
                subLabels.append(labels[i])
        if not subLabels:
            child = Node()
            child.label = Counter(labels).most_common(1)[0][0]
            node.children[val] = child
        else:
            node.children[val] = buildTree(subData, subLabels, remainingFeatures)

    return node

def predict(node, sample):
    """使用决策树预测单个样本"""
    if node.label != -1:
        return node.label
    val = sample[node.featureIdx]
    if val in node.children:
        return predict(node.children[val], sample)
    return 0  # 不应到达

def solve():
    data = sys.stdin.read().strip().split()
    it = iter(data)
    N = int(next(it))
    M = int(next(it))

    thresholds = [float(next(it)) for _ in range(M)]

    trainData = []
    trainLabels = []
    for _ in range(N):
        features = [float(next(it)) for _ in range(M)]
        label = int(next(it))
        discrete = [1 if features[j] >= thresholds[j] else 0 for j in range(M)]
        trainData.append(discrete)
        trainLabels.append(label)

    q = int(next(it))
    testSamples = []
    for _ in range(q):
        features = [float(next(it)) for _ in range(M)]
        discrete = [1 if features[j] >= thresholds[j] else 0 for j in range(M)]
        testSamples.append(discrete)

    root = buildTree(trainData, trainLabels, list(range(M)))
    results = [str(predict(root, sample)) for sample in testSamples]
    print(' '.join(results))

if __name__ == '__main__':
    solve()
```

---

> 完整题目与样例见 [CodeFun2000](https://codefun2000.com/pset/673c82a4d67fed3f6531c8e1)
