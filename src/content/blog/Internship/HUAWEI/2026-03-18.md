---
title: 华为2026-03-18 机试真题（AI方向）
date: 2026-03-18
category: Internship/HUAWEI
tags:
  - 笔试
  - 华为
  - AI
description: 华为2026年3月18日AI方向机试真题，含20道选择题和2道编程题（大模型训练显存优化/KNN语音分类）。
status: published
---

# 华为 2026-03-18 机试真题（AI方向）

来源：[CodeFun2000](https://codefun2000.com)

---

## 第1题-选择题（P4624）共20道，1-15单选，16-20多选

### 1. 下列哪个不是大语言模型（LLM）的典型特点？
- A. 主要基于Transformer架构
- B. 通过海量数据训练获得通用能力
- C. 保证事实的绝对准确性
- D. 参数规模通常达到数十亿至万亿级别

### 2. 下列哪个算法属于监督学习？
- A. 主成分分析（PCA）
- B. 线性回归
- C. K-means 聚类

### 3. 在RLHF流程中，关于奖励模型的作用，最准确的是？
- A. 奖励模型是一个可微分的人类偏好代理，将人类对答案好坏的主观判断转化为可用于优化的连续可导奖励信号
- B. 奖励模型本身就是一个强化学习智能体
- C. 奖励模型的目标是学习模仿人类的写作风格
- D. 奖励模型直接修改基座模型的权重

### 4. 关于先验概率和后验概率，正确的是
- A. 后验概率可以大于1
- B. 后验概率必须通过贝叶斯定理计算
- C. 先验概率是已知条件概率后的修正结果
- D. 先验概率是未考虑新信息前的初始概率

### 5. 下列方法中没有考虑先验分布的是（）
- A. 贝叶斯分类器
- B. 最大似然估计
- C. 贝叶斯学习
- D. 最大后验估计

### 6. MDP中Q-learning更新：s1→a1→s2(终态)，r=3，γ=0.9，α=0.1，初始Q=0。更新后Q(s1,a1)=？
- A. 0.4
- B. 0.3
- C. 0.2
- D. 0.5

### 7. 在高斯混合模型（GMM）中，EM算法的E步主要计算：
- A. 参数更新
- B. 后验概率
- C. 特征函数
- D. 先验概率

### 8. 矩阵B=[[2,1],[0,2]]，下列说法正确的是？
- A. 特征值为1,3，且B不可对角化
- B. 特征值为2,2，且B可对角化
- C. 特征值为1,3，且B可对角化
- D. 特征值为2,2，且B不可对角化

### 9. A=[[1,2],[3,4]]，x=[5,6]^T，则A·x=？
- A. [[17,23],[39,53]]
- B. [[5,6],[15,24]]
- C. [23,34]^T
- D. [17,39]^T

### 10. SVD分解A=UΣV^T，下列说法正确的是（）
- A. V是m×m正交矩阵，列向量是A^TA的特征向量
- B. U是m×n正交矩阵，列向量是A^TA的特征向量
- C. Σ是m×n对角矩阵，对角元素非负且按降序排列
- D. 若A可逆，则Σ对角元素均为0

### 11. 实矩阵A∈R^{m×n}，m≥n，rank(A)=n。一定正确的是？
- A. 若m>n则Ax=b无解
- B. det(A^TA)=0
- C. A的列向量线性相关
- D. 方程A^TAx=A^Tb有唯一解

### 12. A是3阶不可逆矩阵，α1,α2是Ax=0的基础解系，α3是λ=2的特征向量，则不是A特征向量的是（）
- A. 2α1+α3
- B. 3α1-4α2
- C. -5α3
- D. α1+2α2

### 13. 在不丢失信息的前提下使重构误差最小，应采用：
- A. QR分解
- B. LU分解
- C. Cholesky分解
- D. 奇异值分解（SVD）截断

### 14. 在RLHF中，reward model的训练依赖于
- A. 监督数据
- B. 蒙特卡罗采样
- C. 随机初始化
- D. 人类偏好排序

### 15. A=[[-11,4],[-30,11]]，(A+E)(E-A+A²-A³+A⁴-A⁵+A⁶)=()
- A. [[-10,4],[-30,12]]
- B. [[12,4],[30,10]]
- C. [[10,4],[30,12]]
- D. [[-12,4],[-30,10]]

### 16.（多选）关于注意力的表述，正确的是
- A. 并非所有注意力变体都需计算QK^T
- B. MHA的参数量可能不随头数线性增长
- C. MLA的训练速度通常快于MHA
- D. GQA在推理时可减少K/V缓存的内存占用

### 17.（多选）关于连续型随机变量，说法正确的是（）
- A. 概率密度函数在任意点的值可以大于1
- B. 累积分布函数一定是连续函数
- C. 若X是连续型随机变量，则P(X=a)=0对任意实数a成立
- D. 若PDF是常数，则X的分布关于0对称

### 18.（多选）截断误差和舍入误差的区别，正确的有？
- A. 舍入误差源于有限精度表示
- B. 截断误差源于有限步近似无穷过程
- C. 两者均可完全消除
- D. 迭代法中截断误差随迭代减小，舍入误差随迭代累积

### 19.（多选）BPE与WordPiece分词算法，说法正确的是
- A. BPE每轮迭代合并出现频次最高的符号对
- B. WordPiece基于语言模型最大化似然而非简单频次
- C. 两者都会把罕见词切分为更频繁的子词单元
- D. 相同语料训练得到词表一定完全相同

### 20.（多选）关于Causal Mask的作用，正确的是
- A. 防止解码器在训练时看到未来的信息
- B. 是一个上三角为负无穷的矩阵
- C. 用于编码器的自注意力层以增强局部性
- D. 仅适用于文本生成任务

---

## 第2题-大模型训练显存优化算法（P4625）| 中等 | 2000ms

### 题目描述

在大模型训练中，为解决 $NPU$ 显存不足的问题，通常采用张量 $swap$ 或者张量重计算的方式进行优化；张量 $swap$ 是把张量的数据先搬到 $CPU$，需要的时候，再从 $CPU$ 搬回 $NPU$；张量重计算是在需要张量的值时，在 $NPU$ 上重新把张量的值计算出来。

假设把模型部署在 $NPU$ 上，还需要 $m$ 大小的存储空间，才能让大模型运行起来；目前有 $n$ 个候选张量，每个张量占用一定的存储空间；$n$ 个张量中的每个张量都可以进行 $swap$ 或者重计算，$swap$ 和重计算分别对应不同的代价。

试着编写一段程序，从 $n$ 个候选张量中选择合适的张量进行 $swap$ 或者重计算，在代价最小的情况下，使得大模型能够运行起来（选择的张量和大于等于 $m$）。

### 输入格式

- 第一行为 $1$ 个整数 $m(0<m<10000)$，表示需要的存储空间大小
- 第二行为 $1$ 个整数 $n(0<n<10000)$，表示候选张量的个数
- 第三行有 $n$ 个处于区间 $[1, 100000]$ 之内的整数，表示 $n$ 个候选张量的存储空间大小
- 第四行有 $n$ 个处于区间 $[1, 100000]$ 之内的整数，表示 $n$ 个候选张量 $swap$ 的代价
- 第五行有 $n$ 个处于区间 $[1, 100000]$ 之内的整数，表示 $n$ 个候选张量重计算的代价

### 输出格式

如果没有找到合适的解，输出 $error$；否则，输出最小代价（行尾没有空格）。

### 样例1

**输入：**
```
10
5
3 4 5 6 7
1 2 3 5 5
2 3 4 5 6
```

**输出：**
```
6
```

**说明：**

需要的存储空间是 $10$；候选张量长度分别为 $3, 4, 5, 6, 7$；张量长度组合 $3+7, 4+6, 5+6, 5+7, 6+7, 3+4+5, ...$ 都大于等于 $10$，满足运行的基本条件；对于 $3+7$ 来说，$3$ 的最小代价是 $1$，$7$ 的最小代价是 $5$，$3+7$ 的最小代价是 $6$；经过其他组合的比较，达到目标的最小代价是 $6$，所以输出 $6$。

### 样例2

**输入：**
```
12
4
18 20 10 12
12 3 13 4
10 14 10 1
```

**输出：**
```
1
```

**说明：**

需要的存储空间是 $12$；候选张量长度分别为 $18, 20, 10, 12$；张量长度 $18, 20, 12$ 都大于等于 $12$，满足运行的基本条件；其中 $12$ 的代价最小为 $1$，所以输出 $1$。

---

## 第3题-基于KNN的语音数据分类（P4626）| 中等 | 1000ms

### 题目描述

在终端穿戴场景中，往往涉及到用户和终端设备之间的语音交互，通常由用户唤醒相关的设备并进行语音命令控制设备完成相应的操作，设备在收到语音命令时会对用户的意图进行识别，例如识别"拍照""摄像"等意图，从而进行对应的操作。

要求使用 $KNN$ 算法对用户输出的语音特征进行分类：

语音片段经过特征提取后是一个 $3$ 维的向量，所有距离使用欧式距离公式计算，即对于两个 $N$ 维向量：

$$\vec{x} = (x_1, x_2, \dots, x_N), \quad \vec{y} = (y_1, y_2, \dots, y_N)$$

两个向量间的距离为：

$$d_{(\vec{x},\vec{y})} = \sqrt{\sum_{k=1}^N (x_k - y_k)^2}$$

### 输入格式

- 第一行：正整数 $N$ 和 $K$，分别表示给定的已知语音特征向量的数量和邻居数量 $K$
- 第 $2$ ~ $N+1$ 行：已知语音的特征向量和 $label$，其中最后一位表示类别 $label$
- 第 $N+2$ 行：待分类的语音特征向量

### 输出格式

给出待分类语音的类别，该值为正整数在输入给定范围内。

### 样例1

**输入：**
```
10 3
0.5 0.3 0.4 0
0.6 0.2 0.5 0
0.4 0.3 0.3 0
0.7 0.4 0.6 0
2.1 2.3 2.2 1
2.3 2.2 2.4 1
2.2 2.4 2.3 1
4.5 4.3 4.4 2
4.4 4.5 4.6 2
4.6 4.4 4.5 2
2.2 2.1 2.3
```

**输出：**
```
1
```

**说明：**

待分类的语音特征向量为 $(2.2, 2.1, 2.3)$。计算其与所有已知向量的欧式距离后，最近的 $3$ 个邻居分别是：

- $(2.1, 2.3, 2.2)$ 类别 $1$
- $(2.3, 2.2, 2.4)$ 类别 $1$
- $(2.2, 2.4, 2.3)$ 类别 $1$

这 $3$ 个邻居都属于类别 $1$，因此待分类向量被判定为类别 $1$。

### 样例2

**输入：**
```
12 4
1 0.9 1 1
0.8 0.9 0.7 1
1.3 1 1.2 1
1.2 0.9 1 1
2 2.2 2.1 2
2.3 2.2 2 2
2 2.2 1.9 2
1.9 2.2 2.1 2
3.1 3.1 3 3
2.8 2.9 3.1 3
2.9 3 3.2 3
3.1 3 3.1 3
2.2 1.2 1.9
```

**输出：**
```
2
```

**说明：**

与 $(2.2, 1.2, 1.9)$ 最近的 $4$ 个邻居中，类别最多的是 $2$，故被分类为 $2$。

---

> 完整题目见 [CodeFun2000](https://codefun2000.com/pset/673c82a4d67fed3f6531c8e1)
